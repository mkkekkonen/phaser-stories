import Phaser from 'phaser';
// const sheet = require('../assets/snake.png');

export const loadImages = (loader: Phaser.Loader.LoaderPlugin) => {
  // loader.spritesheet('spritesheet', sheet, {
  //   frameWidth: 16,
  //   frameHeight: 16,
  // });
};
